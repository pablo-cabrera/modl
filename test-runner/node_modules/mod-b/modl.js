(function(global) {
    "use strict";

    var node = (typeof exports !== "undefined" && global.exports !== exports),
        modl = node ? require(process.cwd() + "/lib/modl") : global.modl;

    modl.
    require("mod-c").
    exports(function(modl, imports) {
        modl.exports.name = "mod-b";
        modl.exports["mod-c"] = imports["mod-c"];
    });
}(this));